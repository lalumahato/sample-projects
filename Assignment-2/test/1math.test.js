'use strict';
const chai = require('chai');
const assert = chai.assert;

describe(('Math Test'), () => {
    it(('Should retun 9'), () => {
        assert.equal(9, 9);
    });

    it(('Should return 5'), () => {
        assert.equal(2 + 3, 5);
    });
});
